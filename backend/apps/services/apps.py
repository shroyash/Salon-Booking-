from django.apps import AppConfig


class ServicesConfig(AppConfig):
    """Configuration for the services domain application."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.services'
    label = 'services'
